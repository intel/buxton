- make install: create some default data path

- obtain smack label by using smack_new_label_from_socket (sys/smack.h)
  store this in client_list_item after identifying the socket

- add more serialization helpers (network ordering and packet padding)

- begin implementing wire-protocol

- change get_value to no longer use a layer arg, figure out layer based
  on priority instead (William)
